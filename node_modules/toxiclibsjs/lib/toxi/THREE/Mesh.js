define([
    '../internals/extend'
], function( extend ){

    var __firstHasBeenConstructed = false,
        global = this,
        extendTHREEMesh;

    extendTHREEMesh = function(){
        extend( Mesh, global.THREE.Mesh );
    };

    var Mesh = function( triangleMesh ){

        if( !__firstHasBeenConstructed ){
            if( !THREE ){
                return;
            } else {
                extendTHREEMesh();
            }
        }
        __firstHasBeenConstructed = true;
    };


});
